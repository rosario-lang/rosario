procedure Main() is
begin
    let first_value: Int32 = 0;
    let second_value: Int32 = 1;

    let mutable third_value: Int32 = first_value + second_value;
    third_value = first_value - second_value;
    third_value = first_value * second_value;
    third_value = first_value / second_value;

    let mutable array_example: Array<range 2..15 of Int32> = [1, 2, 3, ..];

    let function_call = Hello_Nada();
end Main;

public function Hello_Nada() => Int32 is 2 + 2;
